#include<bits/stdc++.h>
#include "organism.h"
using namespace std;

// target function
double f(double x) {
    return 1.0;
}

// global parameters - maybe put into a separate file?
const int POP_SIZE = 100;
const vector<Organism> POPULATION(POP_SIZE);

double cost(const Organism &o) {
    // calculate how good an organism is
    return 1.0;
}

// fucntions
void createRandomOrganism() {
    /* returns random organism, used for initializing population */
}

void initializePopulation() {
    /* creates initial population */
}


int main() {


    return 0;
}